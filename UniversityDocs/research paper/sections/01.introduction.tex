\section{Introduction}

Recent years have shown that machine learning has immense potential for solving tasks that have been challenging to accomplish using traditional algorithms. This led to creation of many general models, such as Large Language Models, as well as specialized ones for one task.

This has led to wide spread adoption in use of those models. While many of them have adopted a centralized approach which utilized large data centres efficiently processing queries received from users all across the world, this approach also has it's limitations. Some of the main ones, are the need for the device to communicate over internet, which also adds latency, as well as privacy concerns.

Edge AI tries to improve those limitations, by miniaturizing the models, which allows them to be run on consumer devices. A subset of it, TinyML, focuses on the smallest networks, which can be deployed on microcontrollers. This introduces various limitations. MCUs lack the processing power required to run large models, and even memory to be able to save their weights. A microcontroller usually provides us a single, or dual-core CPU with a frequency below 240 MHz, and 512 KB RAM. The latency of the model is also important, as many sources of data for this application come in streams, like audio, or video data, so it would preferably be able to process it in real time.

There have been multiple machine learning architechtures created over the years such as Multi Layer Perceptrons (MLP), Convolutional Neural Networks (CNN), Recurrent Neural Network (RNN), or Transformers. Recently a new one has been proposed - Mamba architecture \cite{mamba} - and has emerged as a compelling alternative to the dominant Transformer models. It works by combining advantages of CNNs and RNNs, which come with many different advantages, including a smaller memory footprint and robust performance on long-context tasks. It achieves that by utilizing a Selective State-Space Model (S4) block.

Optimizing Mamba architecture has been explored with Quantization \cite{}, Prunning, and Knowledge Destilation \cite{} for normal deployment. Implementing and running inference on MCUs has also been achieved in MambaLite-Micro \cite{} paper. The authors of it replicated directly the pytorch inference code for mamba model in pure C code and run it on ESP32S4 and STM32 microcontrollers. This approach makes it possible to achieve full compability with the pytorch version, but also comes with many drawbacks. Especially with difficulties for introducing changes in architecture, or introducing optimizations like the ones mentioned previously.

This paper tries to address this gap in research by answering a question: How can Mamba be improved for microcontroller inference. This is done by investigating possible ways to deploy this model, looking for bottlenecks both in memory usage, as well as latency. Additionally we check if the optimizations introduced for GPU inference have positive impact on MCU inference.

The \autoref{} describes in more detail previous works done on this topic. Then in \autoref{} we explain how the experimental setup was chosen. Next in \autoref{} we describe the deployment pipeline of our mamba model. In \autoref{} we describe the results of the conducted experiments. Next in \autoref{} we go over the ethical implications of our work. After this in \autoref{} we interpret the results. Finally in \autoref{} we conclude the research and give future work recomendations.
